---
# vars file for zabbix_server
_zabbix_server_requirements:
  Debian:
    - python-mysqldb
  RedHat:
    - MySQL-python

zabbix_server_requirements: "{{ _zabbix_server_requirements[ansible_os_family] }}"
